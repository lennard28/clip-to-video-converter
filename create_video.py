def create_video(links: list[str]) -> str:
    """Get a list of links and generate a video from it."""
    check_links(links)
    return "TODO"


def check_links(links: list[str]) -> None:
    for link in links:
        pass